.medium-6.columns.end
  = link_to recipe do
    .banner{style: "#{background_banner(recipe)}"}
      .highlight-text
        %h4
          = recipe.name
          %small.dark
            (
            posted by:
            = recipe.user.username
            )
        = recipe.description
