.medium-6.columns.end
  = link_to recipe do
    .banner{style: "#{background_banner(recipe)}"}
      .highlight-text
        %h4
          = recipe.name
          %small
            (
            Swapper:
            = recipe.user.username
            )
        = recipe.description
