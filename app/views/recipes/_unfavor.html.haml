%div{id: dom_id(recipe, "favor_form")}
  = button_to favoritism_path(current_user.find_favoritism(recipe)),
  params: { recipe_id: recipe.id }, class: "favor_form_button",
  method: :delete do
    %i.red-text.fa.fa-heart
