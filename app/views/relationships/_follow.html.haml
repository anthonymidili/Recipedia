= form_with model: current_user.relationships.build, local: true do |f|
  = render 'notifications/error_messages', object: f.object
  = f.hidden_field :followed_id, value: other_user.id

  .actions
    = f.submit 'Follow', class: 'button'
